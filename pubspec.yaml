name: global_task_planner
description: A smart task planner.
publish_to: 'none'
version: 1.1.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.2.3
  in_app_purchase: ^3.1.13
  flutter_local_notifications: ^19.4.0
timezone: ^0.10.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/icons/gtp_icon_1024.png
    - assets/icons/gtp_icon_512.png
